<Window x:Class="WpfApp7.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WpfApp7.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <d:Window.DataContext>
        <vm:MainWindowViewModel/>
    </d:Window.DataContext>
    <ItemsControl ItemsSource="{Binding Users}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border Padding="10" BorderThickness="1" BorderBrush="Black" Margin="3" CornerRadius="10">
                    <StackPanel>
                        <TextBlock>
                            <Run Text="{Binding Id}"/>
                            <Run Text=" | "/>
                            <Run Text="{Binding Name}"/>
                        </TextBlock>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Score, StringFormat='Score: {0}'}"/>
                            <Button Content="+" Command="{Binding DataContext.IncreaseScoreComand, 
                                RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Margin="10,0"
                                    Padding="2"/>
                            <Button Content="-" Command="{Binding DataContext.DecreaseScoreComand, 
                                RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}" 
                                    CommandParameter="{Binding}"
                                    Margin="10,0"
                                    Padding="2"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</Window>
